[package]
name = "codex-es-guard"
version = "0.1.0"
edition = "2021"

[dependencies]
endpoint-sec = { path = "../endpoint-sec" }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
